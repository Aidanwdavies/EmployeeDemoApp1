<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="EmployeeDemoApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:EmployeeDemoApp"
    Shell.FlyoutBehavior="Flyout">

    <!-- Flyout Menu Items -->
    <FlyoutItem Title="Home" Icon="home_icon.png">
        <ShellContent ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Dashboard" Icon="dashboard_icon.png">
        <ShellContent ContentTemplate="{DataTemplate local:Dashboard}" />
    </FlyoutItem>


    <FlyoutItem Title="Inventory" Icon="jobs_icon.png">
        <ShellContent ContentTemplate="{DataTemplate local:Inventory}" />
    </FlyoutItem>

    <FlyoutItem Title="Customers" Icon="jobs_icon.png">
        <ShellContent ContentTemplate="{DataTemplate local:CustomerPage}" />
    </FlyoutItem>

    

</Shell>
